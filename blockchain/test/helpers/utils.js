// blockchain/test/helpers/utils.js
// Shared test utility functions
// Purpose: Reduce boilerplate in test files; centralize common operations
//
// These utility functions will be populated in Sprint 2:
//
// generateTestUid(prefix) — Generate a unique certificate UID for testing
// generateTestHash(content) — Create a SHA-256 hash from content string
// expectRevertWithCustomError(promise, contract, errorName) — Custom error assertion helper
// logGasUsage(tx, functionName) — Log gas usage for budget verification
//
// Sprint 2 will populate these based on actual contract interface

module.exports = {
  // Utilities exported here in Sprint 2
};
